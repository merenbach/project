{% load i18n %}

<h1>{% trans "Tags" %}</h1>
<div class="roundbox">
    <ul class="tags">
        {% for tag in tags %}
        <li><a href="{% url blog_archive_tagged tag=tag %}">{{ tag }}</a></li>
        {% endfor %}
    </ul>
</div>
